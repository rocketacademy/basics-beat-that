//Global Variable
var userName = [];
var userNumber = 0;

var userAdded = function (user) {
  userNumber += 1;
  userName.push(user);
  return `${user}, welcome to the game. There is ${userNumber} number of player now!`;
};

var main = function (input) {
  return;
};
